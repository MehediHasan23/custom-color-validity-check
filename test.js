/* function isValidHexCode(color) {
  if (color.length !== 7) return false;
  if (color[0] !== "#") return false;
  color = color.substring(1);
  return /^[0-9A-Fa-f]{6}$/i.test(color);
}
let res = isValidHexCode("#fffff3ddd");
console.log(res); */
